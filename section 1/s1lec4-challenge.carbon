package sample api;

fn Main() -> i32 {

    var nameInfo : (String, String, String);

    nameInfo = ("John", "Phillip", "Baugh");

    Print(nameInfo[0]);
    Print(nameInfo[1]);
    Print(nameInfo[2]);

    return 0;
}
